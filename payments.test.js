describe("Payments tests (with setup and tear-down)", function () {
  beforeEach(function () {
    // initialization logic
  });

  it("should ... on createCurPayment()", function () {});

  it("should ... on appendPaymentTable()", function () {});

  it("should ... on updateSummary()", function () {});

  afterEach(function () {
    // teardown logic
  });
});
